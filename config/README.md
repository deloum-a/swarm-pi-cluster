# DNS config

For when the pi-hole config is not quite done or there are issues with the current setup

## Mac OS
```
sudo nano /etc/hosts
```

## Windows
```
```

# SSH config

```
nano ~/.ssh/config
```
